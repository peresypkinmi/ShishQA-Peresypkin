1.Посмотреть, сколько клиентов из Германии (SELECT, WHERE, COUNT).
request:
SELECT COUNT(*)
FROM [Customers]
WHERE Country = 'Germany';
response:
COUNT(*)
11


2. Посмотреть, имена скольких клиентов оканчиваются на на 'e' (LIKE).

request:
SELECT COUNT(*)
FROM [Customers]
WHERE CustomerName LIKE '%e';
response:
COUNT(*)
14


3. Прислать запрос и скриншот с Вашим именем, вставленным в таблицу
 Customers (INSERT, SELECT).

INSERT INTO Customers (CustomerName)
VALUES( 'Mikhail Peresypkin');


4. Изменить собственное имя, вставленное в таблицу Customers
 (UPDATE, SELECT), сделать скриншот и прислать запрос.
UPDATE Customers
SET CustomerName = 'Mikhail Aleksandrovich'
WHERE CustomerName = 'Mikhail Peresypkin';

проверка:
  SELECT CustomerID, CustomerName
FROM Customers
WHERE CustomerName = 'Mikhail Aleksandrovich';


5. Удалить всех клиентов из города Nantes. 
Прислать запрос и результат (DELETE).

DELETE
FROM Customers
WHERE City ='Nantes';


SELECT CustomerName, CustomerID
FROM Customers
WHERE City = 'Nantes';

No result


6. Прислать номера заказов (OrderID), оформленных для клиентов 
работником с именем Steven (FirstName) (INNER JOIN).


 SELECT Employees.EmployeeID,OrderID,FirstName
FROM Employees JOIN Orders ON Employees.EmployeeID = Orders.EmployeeID
WHERE FirstName = 'Steven';







